"use client"

// AspectRatio — keeps a box locked to a shape (e.g. 16:9 or square) as it
// resizes. Wrap an image or video so it never stretches awkwardly.

import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
