// PostCSS for the showcase harness — Tailwind v4 runs as a PostCSS plugin.
// The theme itself lives in the repo-root styles.css, not here.

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
}

export default config
